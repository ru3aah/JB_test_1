meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
run: python bot.py
  scriptName:
  persistenceMount: /data
  containerPort: "80"
